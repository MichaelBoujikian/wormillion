# HANDOFF.md fresh-session read - 2026-09-18

Repo: C:\Users\smite\repos\wormillion, branch expansion-2, HEAD af0d8e9, tree clean at start.
Method: read HANDOFF.md start to end as a newcomer, run every command "Start here" names, open every file it points at, check every offline-checkable claim. No repo edits, no state-changing git.

Progress: complete (see the end of the file).

## Findings (added as confirmed)

### Git state (checked)
- OK: `main` = `8ecb7e5` = tag `v1.3-europe-wave` = `origin/main`; `expansion` = `origin/expansion` = `8ecb7e5` (retired, equal to main, as the file says); `expansion-2` = `origin/expansion-2` = `af0d8e9` = main + exactly one commit; ff from main is possible; `ffeceaa` and `1eaa3fb` exist. All 56 seven-char shas named in HANDOFF.md resolve to commits on `main`.
- MINOR (Start here 1 / Where things stand): "`expansion-2` ... is `main` + this file's release edits" / "this file's edits are its first commit" - the one commit `af0d8e9` also adds four new files (`scripts/expansion/{stack,try,jackpot-share,draw-diff}.mjs`, 165 lines). Not only doc edits; a newcomer diffing main..expansion-2 sees code. Say so in step 1.

### Start here step 1: the three commands (run)
- OK: `npm test` -> 193 pass / 0 fail. `npm run validate` -> "OK - 14901 entries validated." `npm run gap-check` -> 812/812, "Every obvious answer lands".
- WRONG NUMBERS (Where things stand, the category table, column "now"): rivers 3,707 / lakes 1,234 / mountains 2,630 / islands 2,393 sum (with seas 571, deserts 137, cities 3,798, capitals 247, countries 197) to **14,914** - the pre-audit bank. `validate` today prints rivers **3,703**, lakes **1,232**, mountains **2,625**, islands **2,391** = 14,901. The table was not updated for the 13 rows the pre-merge audit dropped (4 rivers, 2 lakes, 5 mountains, 2 islands). Seas/deserts/cities columns are right.
- STALE (Things the user has said they care about): "`gap-check` is the guard (338 answers)" - it is 812 today (the file's own "Where things stand" says 812).
- MINOR (running log, lakes): "Lakes done: cohort 1,234 (was 1,157)" vs the table's "US wave end 1,160" for lakes. Explicable (the three wrong-article lakes dropped in between) but not explained.
- MINOR (running log, lakes): "60 Nordic rows at exactly one view a day" vs commit `8f12b0d`'s subject "62 rows at exactly one view".

### The four tools (run with the file's example invocations)
- OK: `node scripts/expansion/try.mjs '{"category":"capital","region":"Oceania"}' Auckland Honolulu` -> Auckland `elsewhere:city:city-auckland`, Honolulu `accepted:capital-honolulu 49221 views/mo` (matches "Honolulu carries Oceania"); `--ref=main` gives the same on the main stack.
- OK: `jackpot-share.mjs` prints all nine cohorts; desert 11 of 137 = 8.03% (file: "8% (11 of 137)"); `--list=desert` lists the 11. The "Known >=85% answers" list (Micronesia, South Tarawa, Bel Air South, Karpa Island, Storvindeln Lake, Cerro Fabrega, Mecaya, Skeleton Coast, Gulf of Manfredonia...) matches the tool's lowest rows.
- OK: `draw-diff.mjs main` -> "0 of 46 days ... draw differently (main vs working tree)" - correct, expansion-2 has no engine/bank change yet. `draw-diff.mjs ffeceaa --from=2026-09-17 --days=50` reproduces the merge-window list exactly: 09-19 rounds 4-15 (r8 excepted), 09-20 r14, 09-23 r9, 09-24 r9, 10-02 r10, 10-09 (r3-9,11,14 - the file says "r3-14", loosely), 10-20 r13, 10-21 r15, 10-28, 10-30, 11-01. Digs #6 and #7 identical on both.
- USAGE NOTE (The loop / tools paragraph): the file gives `jackpot-share.mjs` `--list=<category>` but not `--ref=<git-ref>` (the tool has it); `draw-diff.mjs` takes `--from=` and `--days=` (the tool's header shows them; HANDOFF does not). Minor - the tools' own headers are complete.
- INCONSISTENT (Where things stand 4): "the desert cohort's jackpot share is 8% ... the one cohort outside the 1.6-4% band". Today's numbers: country 14.21%, capital 5.26%, mountain 1.41% are also outside that band. "Every knob" says "jackpot share per cohort 0.8-4.0%" (dated 2026-09-16) - also not true of country/capital. The band claim only holds for the six expanded physical cohorts + city; say so.
- NOTE: `draw-diff.mjs` defaults `--from` to today's UTC date; run on 2026-09-17 local it starts at 2026-09-18. Harmless, but "the next 46 days from today (UTC)" is only in the tool header, not HANDOFF.

### "Every knob" table vs the source (checked)
- OK: `MIN_ELIGIBLE` 6, `MAX_ELIGIBLE_SHARE` 0.7, `MAX_ELIGIBLE_SHARE_LETTER` 0.6, `OPENING_ROUNDS` 2, `MODIFIER_CHANCE_START/END` 0.7/1.0, `options.push('flag','flag')` (region also x2, size x1), `SIZE_RULES.city` 500k/1M/5M/10M, `LETTER_ONLY_FILLER.river` = creek/fork/branch/run/brook/kill/wash/slough/draw, `WHOLE_NAME_CATEGORIES` = country/capital/city/sea_ocean, `LETTER_FILLER` = FILLER minus loch/lough/llyn/saint/st/cape/rio + the ten foreign words (promptBank.js 78-159). `matching.FILLER` is exactly the list in the table incl. reservoir and the ten foreign words; `slackFor` 0/1/2/3 at <=4/<=7/<=11/else; `normalize` folds hyphen->space, okina->apostrophe, Mt->Mount, thorn->th (matching.js 23-47, 133-138). `rarity.js`: DIG_SCALE 70, JACKPOT_RARITY 0.85, DIG_JACKPOT 75, DIG_PERFECT 100, PERFECT_RARITY 0.995, POINTS 50-1000 gamma 1.4, POINTS_JACKPOT 950, DUD_RARITY 0.005, budget 15x100. `seed.js` DAILY_EPOCH 2026-09-12. `lib/daily.js` OPEN_BEFORE 1 / OPEN_AFTER 2. `HISTORY_CAP` 50. `REMOTE_API` and `CANONICAL_URL` as stated. `COMPARE_FRESH_MS` 60 s, edge `max-age=60`. `HOLD_SECONDS` 10 in both overlays, `FLIES` 5, `TUNNEL_R` 6 / 11 / 14, `TAPER_UNITS` 6, Core from 600. `fetch-pageviews.mjs` DAYS 60.
- IMPRECISE (knob "CORS origins"): "Netlify itself, https://michaelboujikian.github.io, http://localhost:8123" - `ALLOWED_ORIGINS` in `lib/netlify.mjs:11` holds only the last two; the Netlify origin is same-origin and never needs the header. Harmless.

### The decision-5 backlog: `work/*.report.txt`, `*.out` (Start here 2)
- MISSING STEP / MACHINE-BOUND: `scripts/expansion/work/` is **gitignored** (`.gitignore` line 6) - 0 files tracked, 250 files on this disk. The file says the backlog "is already on disk (the three waves' 'taken' lists)" and the design says the namesakes "come straight back from the probes' 'taken' lists with their views". True on this machine only. HANDOFF names `scripts/.cache/` as gitignored (pipeline section) but never says `work/` is; a session on another clone has no backlog, no probe caches, no `wikitext-cache.json`, and would have to re-run every probe (hours, one resolver at a time). Say plainly: `work/` and `scripts/.cache/` are local-only; if they are gone, the taken lists must be regenerated with `probe.mjs` (cached tree/resolve/sizes are gone too).
- OK: the files exist here. `*.report.txt` exists only for the US probes (`us-{cities-50k,deserts,islands,lakes,mountains,rivers,seas}.report.txt`); the Europe probes wrote `<name>.out` (`uk-rivers.out`, `eu-cities.out`...). Each has a "==== NAME TAKEN (n) - typing the name lands on a different place ====" section with views/mo, size, the Wikipedia title (with its comma/parenthetical qualifier) and the bank holder - exactly what the design needs.
- TERM: "the three waves' 'taken' lists" (Start here 2, and "Sequence") - the file elsewhere counts two waves (US, Europe). A newcomer cannot tell what the third is (the 2026-09-15 coverage probes? the US no-figure re-runs?). Name the probe files instead.
- MISSING STEP (design, Data): the taken lists are not all namesakes. `eu-cities.out` NAME TAKEN holds "City of Westminster" (a council article, taken by Westminster CO), "City of Bradford/York/Sheffield..." (district articles), "Hagen" (taken by Mount Hagen - a different cohort, an `elsewhere` case), and `us-cities-50k.report.txt` has "St. George, Utah" (taken by George, South Africa through the `st` filler), "Canton, Ohio" (Guangzhou's alias), "Carson" (Carson City), "Palm Desert" (Palm Bay?!), "Odessa, Texas" (Odesa). The fold of the backlog needs a hand pass; the design does not say so, and the "~180" count is not derivable from the files (85 + 37 city rows alone = 122 before cleaning; plus the physical cohorts' taken lists).

### "Same-name places: the design" - what an implementer cannot act on
- CONTRADICTION (design, Matching rule 2, the "if none" example): the rule says "take the ones that satisfy the current prompt; if exactly one, that is the answer ... if none, wrong-scope on the most famous, and the message names the alternative" - and then illustrates the *none* branch with "Syracuse, New York isn't in Europe - for the Sicilian one type Syracuse Sicily". On a Europe round the Sicilian Syracuse *does* satisfy the prompt, so by the rule it is the exactly-one case and is accepted silently; the message can never appear on that round. The example needs a scope neither fits (Africa), and then "for the Sicilian one type..." makes no sense. Which behaviour is wanted on a Europe round is exactly the question the implementer must not guess.
- CONTRADICTION (Standing decisions "all still in force" #4 vs Decision 5): #4 says "Name-taken places stay out ... No comma-form or parenthetical names" and is listed as in force; "Where things stand" #2 and the design say the ~180 name-taken places are the next job to fold. #4 needs "until decision 5 lands; then a namesake goes in with a qualifier".
- CONTRADICTION (Standing decision #4 / design "Options set aside" vs the bank): "No comma-form or parenthetical names" and "the wave rule bans comma names" - but `bank.js` ships **23 parenthetical names** (`Derwent (Derbyshire)`, `Stour (Kent)`, `Krka (Croatia)`, `Grand (Ontario)`, `Min (Sichuan)`, `Tana River (Kenya)`, `Black Desert (Egypt)`, `Green Island (Taiwan)`, `Saint John (New Brunswick)`, `Avon (Western Australia)`...). "Decisions a new session should not undo" acknowledges them ("only there when Wikipedia's own title has it and the bare name is taken in the cohort") - but for Krka, Rewa, Tapi, Scamander, Black/White/Red Desert, Fitzroy, Fish River, New River, Saint Paul River the bare name is NOT taken by anyone. The design is silent on these 23 rows, which are precisely the "qualified bank names" option it sets aside: today `Derwent (Derbyshire)` counts as a long name (17 letters) and `Krka`, `Scamander`, `Black Desert`, `White Desert`, `Red Desert` typed bare are **unrecognized** (no bare alias; `try.mjs` verified). Decision 5's data step should migrate them to name + qualifier; say so.
- UNDEFINED (design, Data): "Every namesake gets a qualifier, the famous one too" - Wikipedia supplies the qualifier only for the *newcomer* (the comma part of "Syracuse, Sicily"). The incumbent's title is often bare (Boston, Birmingham, Cambridge, Manchester, Plymouth, Frankfurt, Guadalajara, York, Sheffield, Halifax = "Halifax, Nova Scotia" is fine, but Boston = "Boston"). Where the incumbent's qualifier comes from (hand-written? state/country column? `data-countries` country?) is not said. For physical cohorts (Green River KY vs the bank's Green River, Black Lake x3, Colorado River TX) the same.
- UNDEFINED (design, Data): where the `qualifier` column sits in the pipe formats (`Name|magnitude|aliases` in `data-physical.mjs`, `Name|Country|population|aliases` in `data-cities.mjs`, `Name|size|aliases|wikiTitle|themes` in a fold chunk) and how it is spelled in `bank.js` (a runtime field? SPEC 4 lists the runtime fields; `qualifier` would have to be added there and in `build-data.mjs`'s field list).
- UNDEFINED (design, Matching): the fuzzy pass refuses a tie between two entries. Two namesakes are always the same distance from a typo ("Syracus" -> both Syracuses) - unless the tie rule is told that entries sharing a bare name are one candidate, every typo of a namesake becomes a refused tie. Not stated.
- UNDEFINED (design, Matching): "Portland, OR" as an exact hit needs US state abbreviations as qualifier aliases; no table exists (`chunk.mjs` has `US_STATES` names only) and the design does not say which forms are generated ("Name Qualifier", "Name, Qualifier", abbreviations, "Name (Qualifier)").
- UNDEFINED (design, rule 5): "say 5x the views" - a knob with no name, no file, and no rule for the tie ("famous holder lives in another cohort by a wide margin": measured against the most-viewed in-cohort namesake or against the one typed?). Also its interaction with the standing rule "a bare name that belongs to a far more famous place *not in the bank* is not free (Athens GA)" - with a qualifier column Athens GA could go in as `city-athens-georgia`; the file does not say whether decision 5 admits that class or keeps it out.
- INCOMPLETE (design, rule 6, tooling list): "themes.js keys members by name ... a small tooling change (add-theme.mjs, drop.mjs, fold.mjs, build-data.mjs)". Also keyed by name and not listed: `scripts/data-wiki-titles.mjs` (`WIKI_TITLES`/`WIKI_VERIFIED`), `scripts/data-oceans.mjs` `OCEAN_OVERRIDES`, `src/data/pageviews.json`, `scripts/gap-check.mjs`'s pins, `fix-titles.mjs`, `add-alias.mjs`, `add-oceans.mjs`, `append-row.mjs`, `range-tag.mjs`, `validate-data.mjs`'s "listed twice" check in `build-data.mjs:151`. (Checked below which are keyed by what.)
- INCOMPLETE (design, "What it costs"): the incumbent's id changes (`city-syracuse` -> `city-syracuse-new-york`). Nothing the client persists carries ids (records store names) and the server replays names, so it is safe - but the file should say that, since the first thing an implementer asks is "what breaks when an id changes".
- MISSING STEP (design, Data): the fold "comes straight back from the probes' taken lists" - the taken lists need a hand pass first (see above: council articles, filler artefacts like St. George->George, Canton->Guangzhou, Hagen->Mount Hagen) and `chunk.mjs` already has `--include-taken` (not mentioned anywhere in HANDOFF).
- MISSING STEP (design, sequence): "one resolver window for views" - the views for the namesakes are already in the probe caches (`work/<name>-cache.json`, the taken lists print views/mo); what still needs the network is `fetch-pageviews` for the new rows' `pageviews.json` entries and coordinates. Say which.
- CORRECTION to the two "INCOMPLETE" items above, after checking the keys: `scripts/data-wiki-titles.mjs` (`WIKI_TITLES`, `WIKI_VERIFIED`), `scripts/data-oceans.mjs` (`OCEAN_OVERRIDES`) and `src/data/pageviews.json` (`entries`) are keyed by **id** (`'country-georgia': ...`, `'island-sumatra': [...]`, `"country-nigeria": {...}`), and `build-data` refuses to run if any entry lacks pageviews. So "every namesake gets a qualifier, the famous one too" + "the id becomes `city-syracuse-sicily`" means the incumbent's id changes and its `WIKI_TITLES` override, its ocean override and its `pageviews.json` row all go stale at once (a re-fetch or a key migration per renamed row). This is the largest hidden cost of the symmetric-qualifier choice and the design's "What it costs" does not mention it. `themes.js` (by name) and `gap-check.mjs` (typed names) are the smaller half.

### Stale sections and dangling references
- DANGLING + CONTRADICTION (Things the user has said they care about): "Pushes to `main` are allowed without asking (but see \"Raise this first\")" - there is no section "Raise this first" anywhere in the file (grep: one hit, the reference itself), and the sentence contradicts the header, Start here 4 and standing decision 1 ("never push to `main` except as a release"). Delete or rewrite as "a push to main is a release; ask".
- STALE (Also queued, lower): "`river-humber` scores on Humber River (Ontario) while themed as England's" - fixed in `9813fd5`; today `river-humber` -> title "Humber", 5,114 views (`pageviews.json`, `bank.js`). Same in "Lessons the US wave paid for": "Humber (Ontario's, themed as England's - still open)".
- STALE (Also queued, lower): "`README.md` says \"1,719 places\"" - README line 32 says 14,900 (updated in `c413d94`). SPEC 6's count table is still at v1.1 numbers (true) and `dist/wormillion.html` is still the Sep 13 bundle (true, and `dist/` is gitignored).
- STALE (Open threads): "652 US bays, ~1,100 US islands, 10 US deserts have no area - decision 1" listed as open; the running log says decision 1's debt is paid (`b296377`, `4d04453`, `0cce2e6`) and the bank has 288 size-0 seas, 379 size-0 islands, 7 size-0 deserts. "Sweden's 164 lakes under 30 km² were left out on purpose" predates the `nordic-lakes` re-run at 25 km² (92 rows) - say what is still out. "55 Colombian rivers have no length figure anywhere" - with size 0 legal since `7d00d78` this is foldable; the file does not say whether it was.
- STALE (running log, last bullet): "Probe configs written for every remaining category (...); seas, deserts and cities still to write" sits *after* "The Europe wave is complete" - `probes/eu-seas.json`, `eu-deserts.json`, `eu-cities.json` exist and were run (the Seas / Deserts / Cities bullets above it). Reads as current state; it is not.
- STALE-LOOKING (running log, decision 1): "**Still owed:** the US no-figure rows ... Needs the resolver slot" - superseded eleven bullets later ("decision 1's debt is paid"). Chronological, but the bold "Still owed" is what a skimmer keeps.
- OFF BY ONE (table, islands): "380 rows `size` 0" - 379 today (one dropped by the pre-merge audit).
- TIMESTAMPS: the file says it was "last rewritten 2026-09-18 ~02:30 UTC" and Netlify was "verified 02:00 UTC"; the commit that contains those words (`af0d8e9`) is stamped 2026-09-17 18:46 -0700 = 01:46 UTC, and the clock at this read is 01:58 UTC. The file's UTC times run ~30-45 min ahead of git. Harmless, but a newcomer checking "is this the latest?" against `git log` will trip on it.
- TERM USED, NEVER DEFINED: "resolver window" / "resolver slot" (Start here 2; running log x2). Meaning inferred from "Timings and the Wikipedia rule": a stretch when none of `probe.mjs` / `article-size.mjs` / `fetch-pageviews` / `auto-titles.mjs` is running, because a stray request 429s the running one. Define it once where it is first used.
- TERM: "the best probe" is explained in standing decision 1 (good), but "the three waves" is not (see above).
- INCONSISTENT (Where things stand / Engine changes): "all in SPEC §13, each with a test" - decision 2 (`MAX_ELIGIBLE_SHARE` 0.7, letter 0.6) is in SPEC 3.1a's body but has **no §13 row**; every other item listed has one.
- INCONSISTENT (SPEC §13 vs code, a file HANDOFF points at): §13's "engine changes not logged above" row says "creek / fork / ... / draw / bayou / arroyo are letter-only filler for rivers"; `LETTER_ONLY_FILLER.river` in `promptBank.js:152` has no bayou/arroyo and its comment says they lead the name like rio. HANDOFF's knob table agrees with the code. SPEC §13 is the odd one out.
- INCONSISTENT (AUDIT-BRIEF.md, pointed at by Start here 2): its size thresholds "rivers 1,000/3,000 km, lakes 100/1,000/10,000 km², mountains 4,000/6,000 m" do not match `SIZE_RULES` (rivers 500/1,000/3,000/5,000; lakes 100/1,000/10,000/30,000; mountains 1,000/3,000/5,000/8,000). It also says "Sonnet is the default model for these; the user names Opus" and "two hundred thousand tokens is a normal budget", while HANDOFF says audits are on Opus (standing decision 5) at 250-440k. And it points auditors at "`README.md` 'Where the logic lives'" - README has no such heading (the table is in HANDOFF; HANDOFF's own last section also says "README's 'Where the logic lives'").
- USAGE GOTCHA (Driving the game from JS; try.mjs): slot shape `{category, letter:{kind,letter}}` - the letter must be **lowercase** (`LETTERS` in promptBank.js); `{"letter":"D"}` prints "starts with D" and then refuses every answer as wrong-scope (Danube included). Verified with try.mjs. Neither HANDOFF nor try.mjs's header says so.
- MINOR (Standing decisions): #1 says "the user's 2026-09-18 instruction: a new branch per phase" but the header says the user accepted decisions "2026-09-16" and "2026-09-17"; and the "Decisions - taken 2026-09-16 evening" list keeps item 5 as "Recommend: not this wave" with no pointer forward to the accepted design (a reader lands on the old recommendation first).
- MINOR (The loop, step 3): `chunk.mjs`'s usage line omits `--include-taken` and `--allow-no-figure` (both exist; the first is what decision 5's fold needs).
- STALE WINDOWS (The release, as audited, Procedure step 1): the safe windows listed ("before 2026-09-18 10:00 UTC; else 09-21 12:00 -> 09-22 10:00; else 09-25 12:00 -> 10-01 10:00") are the *old-bank vs Europe-bank* windows and are all spent or irrelevant for the next release; Start here 4 sends the reader to this procedure. Step 1 should say "run `draw-diff.mjs main` and pick a window from its output" and keep the old dates only as the worked example. (Today `draw-diff.mjs main` prints 0 differing days - there is nothing to release yet.)
- MINOR (Where things stand #2): "It is the next engineering step **after the merge** (it touches the matcher)" - the merge happened; the sentence reads as if it is still pending until the next line.
- OK (checked, no finding): every path in "Where the logic lives" and "Reference" exists; `.claude/launch.json` names `wormillion` on 8123; `play.cmd` exists; `netlify.toml` gates on `npm test && npm run validate` and bundles the two data files; `deploy.yml`/`ci.yml` exist; `__wormillion` exposes renderer/burst/drip/currentRun/start/diveTo/celebrate/shame; `usedAnswers` is by id; `buildLookup` maps key -> one id (the design describes the current shape correctly); the 15 Western Europe + 5 Nordic countries are tagged as stated; `bank.js` is 1,970,246 bytes / 277 KB gzip; 9,300 -> 14,901 ids between `ffeceaa` and HEAD (109 commits in the release push); the polar deserts score on Antarctica/Arctic via `WIKI_TITLES`; `tests/seed.test.js` and `tests/prompts.test.js` pin 2026-09-12; the flag test reads `promptBank.MAX_ELIGIBLE_SHARE`.

## Verdict: could a fresh session start decision 5 from this file alone?

**Partly.** The engine half, yes: the file names the files (`matching.buildLookup` / `matchAnswer`, `run.js`, `promptBank.js`, `ui.js`, SPEC 3.7/4/13), the rules are mostly precise, `try.mjs` and `stack.mjs` give a way to measure, and `npm test` is green to start from. I would begin with the lookup returning candidate lists, the scope pick in `run.js`, the validator change and a test per rule - and stop at the first ambiguity below, which comes within the hour (the Europe-round example).

The data half, no - not without the user:

1. **The Europe-round contradiction.** "Name a city in Europe" + "Syracuse": accept the Sicilian one silently (rule 2) or refuse with "Syracuse, New York isn't in Europe - for the Sicilian one type Syracuse Sicily" (the example)? These are different games.
2. **The incumbent's qualifier and id.** Where does Boston's / Birmingham's / Manchester's qualifier come from (their titles are bare)? And is it acceptable that every incumbent's id changes (`city-syracuse` -> `city-syracuse-new-york`), which strands its `WIKI_TITLES`, `OCEAN_OVERRIDES` and `pageviews.json` rows (all keyed by id) - or should the incumbent keep its id and only the newcomer carry the suffix?
3. **The fuzzy tie between namesakes.** "Syracus" is equidistant from both Syracuses; today a tie refuses. Collapse same-bare-name entries to one candidate before the tie rule, then scope-pick?
4. **Rule 5's knob.** "5x the views" - fixed, or measured against the in-cohort namesake's views? And does decision 5 admit the class standing decision 4 still bans (Athens GA, Dublin CA: the famous holder is *not* in the same cohort) as qualified rows, or do they stay out?
5. **The 23 parenthetical rows already in the bank** (`Derwent (Derbyshire)`, `Krka (Croatia)`, `Black Desert (Egypt)`...): migrate them to name + qualifier in the same change? Five of them are unreachable by their bare name today.
6. **Which typed forms are exact hits** - "Syracuse Sicily", "Syracuse, Sicily", "Syracuse (Sicily)", "Portland Maine", "Portland ME", "Portland OR"? A state-abbreviation table would be new.
7. **The backlog's scope and floors.** Cities only, or also the physical namesakes (Green River KY, Colorado River TX, Black Lake x3, Prince of Wales Island AK, the British Avon x4 / Stour / Derwent / Ouse / Don / Dee)? Same views floors as the wave (rivers 30, lakes 61, islands 91)? Is "~180" the target or a guess?
8. **Sequencing / branch.** Decision 5 on `expansion-2` before Mexico/Canada (Start here) - confirm, and confirm the branch (the user has since asked for a fresh branch for the sweeps).

Also needed but answerable without the user: the `work/` caches exist only on this machine (gitignored), so the backlog is only "on disk" here; the letter in a `letter` slot is lowercase; `chunk.mjs --include-taken` is the switch that emits the taken rows.

Progress: complete. Every Start-here command run; every named file opened; every sha, tag, branch, count, knob and path checked offline. No repo file edited; no state-changing git.
